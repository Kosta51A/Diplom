import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  mapContainer: {
    height: "90vh",
    width: "100%",
  },
  cardImage: {
    height: 85,
    width: 85,
    cursor: "pointer",
  },
}));

export default useStyles;
